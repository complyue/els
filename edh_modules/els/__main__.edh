{##
 # this is the entry module of Đ (Edh) Language Server
 #}

import * 'els/server'

# listen on specified port, of '127.0.0.1' by default
els = ELS( 1707 )
# keep the server process running
els.join()
