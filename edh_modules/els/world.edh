
import * 'els/RT'

export world = MetaWorld()

if debug then {
  import * 'els/debug'

  {
    go elsDebug.replHere()

    thisModule = world.locate( __file__ )
  } $=> { exc } -> {
    console.error<| desc( exc )
  }
}
