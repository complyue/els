
import * 'els/RT'

import * './debug'

export world = MetaWorld()

if debug then {
  {
    go elsDebug.replHere()

    thisModule = world.locateByFile( __file__ )
  } $=> { exc } -> {
    console.error<| desc( exc )
  }
}
